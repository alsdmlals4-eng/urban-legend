# MVP-042 일정 비소모 일상 에피소드 검증

기준: `MVP-042` / `Ver 4.1` / 저장 스키마 `mvp-039`
확인일: 2026-07-14

## 범위와 결론

HQ 준비 화면에서 여는 선택형 요원 대화 3장을 검증했다. 일상 카드는 반일 일정, 현장 조사, 사건 위험도, 조사 편성, 세력 의뢰를 소비하거나 변경하지 않는다. 선택은 즉시 결과와 요원 반응을 보여 주고 기록국 DB에만 남긴 뒤, 같은 HQ 준비 화면으로 돌아온다.

| 카드 | 담당 / 관련 사건 | 노출 조건 | 결과 |
|---|---|---|---|
| `막차 안내판의 빈칸` | 강이준 / 저승역 | `lead`·미해결 | 통과 |
| `젖은 우산 보관함` | 권나래 / 빨간 우산 골목 | `lead`·미해결 | 통과 |
| `무음이 남은 편집실` | 오현 / 폐주파수 방송국 | Day 8 자연 발견 후 `lead`·미해결 | 통과 |

## 자동 검증

- `test_mvp042_daily_episodes.gd`: 28 passed, 0 failed.
  - 데이터 3장·선택지 2개, `lead`/미해결 노출, 최초 1회 완료 제한, 기존 사건별 일상 이해도 +2 및 +10 상한을 확인했다.
  - 시작 중 저장·재개, 완료 기록·DB·보상 저장 왕복, `mvp-038 → mvp-039`의 빈 일상 상태 이관을 확인했다.
  - 일상 진입과 완료 전후의 일자·반일·캠페인 단계·위험도·요원 일정·의뢰 게시판이 변하지 않음을 확인했다.
  - 폐주파수 카드는 자연 Day 8 발견 전 숨겨지고, 앞선 두 사건 해결 후 자연 발견 뒤에만 표시됨을 확인했다.
- `daily_episode_ui_test.gd`: 준비 화면 카드 → 선택 화면 → 즉시 결과 → HQ 복귀 → DB `일상 에피소드 기록` 흐름을 통과했다.
- Godot 종료 시 알려진 `ObjectDB instances were leaked` 경고가 일부 UI 테스트에 남지만, 테스트 결과는 성공이며 새 실패·저장 훼손은 없었다.

## 수동 화면 QA

OpenGL 캡처로 HQ 카드, 선택 직후 결과, DB 기록을 확인했다.

| 해상도 | 확인 결과 |
|---|---|
| 1280×720 | 카드의 일정 비소모·+2 상한·DB 기록 문구, 2개 선택지, 즉시 결과·복귀 버튼, DB 요약이 줄바꿈·가림 없이 읽힌다. |
| 1920×1080 | 같은 세 화면에서 카드와 버튼 포커스, 결과 문단, DB 기록 패널이 한 화면에서 읽힌다. |

캡처는 작업 폴더 `C:\Users\user\Documents\바이브코딩\urban-legend-ui-qa\mvp042`에 보관했다. 자동 캡처는 실제 창 모드에서 수행했고, 1920×1080은 전체 화면으로 고정해 정확한 해상도를 사용했다.

## 문서 배포 검증

- GDD Markdown에서 DOCX를 재생성했고 `--check`는 원본 해시 일치, 문단 210개·표 33개·이미지 5개를 보고했다.
- DOCX 접근성 구조 감사는 high/medium/low 0건이다.
- 이 환경에는 LibreOffice 렌더러가 없어 DOCX의 페이지 PNG 시각 QA는 수행하지 못했다. 게임 화면 QA와 DOCX 구조 감사는 별개이며, 실제 Word/LibreOffice 렌더링 환경에서 페이지 단위 확인이 다음 배포 전 보완 항목이다.

## 벤치마킹·외부 검토

- 서울 2033의 “짧은 독립 사건 + 선택 직후 결과 + 누적 기록” 근거를 그대로 유지했다. 이번 작업은 이미 확정한 구조의 최소 구현·검증이므로 신규 광범위 조사는 하지 않았다.
- DeepSeek `research-scout-flash` R0 읽기 전용 반대검토는 별도 작업트리에서 0개 파일을 변경했다. 일상 상태·기록을 사건 진행과 분리하라는 관찰은 채택했다.
- 반일 일정 또는 경제를 일상에 추가하라는 제안은 사용자 고정 조건(일정 비소모)에 맞지 않아 제외했다. 미확보 정보 누설, 대규모 관계 수치, 분기·육성·엔딩 수집은 도입하지 않았다.

## 후속 후보

- P1 연구·장비·세력 콘텐츠 확장의 선택 이유·가격·보상 QA.
- 일상 카드 총수와 요원 조합별 최소 분량은 실제 플레이테스트 뒤 결정한다. 현재 3장은 메인 추리의 필수 단서나 회수 해법을 대체하지 않는다.

## 작업 구조 기록

- 최신화: GDD·로드맵·README·프로젝트 문맥·체크리스트와 DOCX를 MVP-042 기준으로 갱신한다.
- 사용: `urban-legend-game-workflow`, DeepSeek R0 읽기 전용 검토, DOCX 생성·구조/렌더 검증.
- 중복 방지: MVP-041의 텍스트 노벨 근거를 재사용했으며 새 시장 조사는 생략했다.
- Base 승격 후보: 없음. “일정 비소모 선택 콘텐츠”는 이 프로젝트의 캠페인 계약에 종속한다.
