# D-2026-08-04-AFTERLIFE-STATION-RECURRING-PLATFORM-PERSISTENT-TRACE-ANCHOR

- 제목: 저승역 `[회귀 승강장]` 지속 흔적 기반 잔향 고정
- 상태: `APPROVED_PRODUCT_DECISION / GRILLME_BATCH_4_5_OF_10 / PENDING_BATCH_MERGE`
- 승인 시각: `2026-08-04 09:25 KST`
- 책임 Batch: 저승역 완전 사건 설계 Grill Me Batch 4
- 구현: `NOT_AUTHORIZED`
- Human QA: `NOT_RUN`

## 결정

저승역의 두 번째 대표 회수 패턴은 `[회귀 승강장]`으로 확정한다.

이 패턴은 Decision 2에서 확인한 비대칭을 전투 규칙으로 재사용한다.

```text
사람·괴이 허상·승강장 표지는 반복해서 위치가 초기화
+ 시간·기록·흔적은 유지
+ 복수 초기화 주기의 지속 흔적을 교차 비교
= 위치 초기화와 무관한 실제 잔향 좌표 확인
```

`[회귀 승강장]`은 승차권을 파훼 수단으로 사용하지 않는다. 공식 검표 흔적은 사용할 수 있는 여러 지속 흔적 중 하나일 뿐이며, Decision 4 패턴을 먼저 겪지 않아도 다른 독립 흔적으로 해결 가능해야 한다.

## 전조와 공정성

- 패턴 길이는 `N ≥ 2`의 가변 전조 계약을 따른다.
- 최종 발현 전 최소 2개의 초기화 주기와 최소 2종의 독립 지속 흔적을 제공한다.
- 위치가 되돌아갈 때마다 타임스탬프·좌표·연속 로그·잔향 자국의 차이가 누적된다.
- 같은 위치에 겹쳐 나타나는 괴이 허상은 정답 표식이 아니다.
- 실제 잔향 좌표는 여러 주기에 걸쳐 사라지지 않고 일관되게 남은 흔적의 교차점으로 추론한다.

## 정확한 대응

플레이어는 최종 턴 전에 누적된 기록을 비교하고, 최종 턴에 `지속 흔적 고정`을 선택해 실제 잔향 좌표를 지정한다.

```text
지속 흔적 선택
→ 실제 잔향 좌표 확인
→ 잔향 고정
→ 전체 위치 초기화 실패
→ [파훼]
→ 잔향 노출
→ 괴이 [취약]
```

매뉴얼이 실제 잔향을 자동 표시하지 않는다. 정답 좌표를 자동 선택하지 않는다. 플레이어가 기록을 비교하고 좌표를 지정해야 한다.

## 대체 대응 결과

- 초기화된 괴이 허상 공격: 빗나감과 반격
- 방어: 초기화 피해 감소, `[취약]은 발생하지 않는다`
- 이미 사라진 흔적 선택: 잘못된 좌표 고정과 위치 초기화
- 흔적이 충분히 쌓이기 전 조기 고정: 행동만 소비하고 파훼 효과 없음

모든 결과는 숨겨진 확률 없이 동일 입력에 동일 결과를 반환한다.

## 접근성

- 초기화 주기마다 타임스탬프와 좌표를 텍스트로 남긴다.
- 소지품·검표 흔적·잔향 자국에는 형태 설명과 출처 라벨을 제공한다.
- 연속 녹음은 재생 시간과 문자 기록을 함께 제공한다.
- 화면 효과만으로 판별하지 않는다.
- 음향만으로 판별하지 않는다.

## Guardrail

- 단일 빛나는 흔적을 클릭하는 정답 찾기로 축소하지 않는다.
- 첫 대표 패턴과 같은 승차권 제시를 반복하지 않는다.
- 공식 검표 흔적이 없으면 해결할 수 없는 순서 의존성을 만들지 않는다.
- 괴이 허상 공격만으로 실제 잔향을 노출하지 않는다.
- 방어와 정확한 고정에 같은 보상을 주지 않는다.
- 매뉴얼·접근성 기능이 정답 좌표를 자동 추천하지 않는다.

## 비결정 범위

- 실제 턴 수 분포와 흔적 개수
- 좌표 UI와 기록 비교 화면
- 피해량·취약 지속 시간
- 카메라·애니메이션·VFX·SFX
- 코드·Scene·전투 데이터·자산 구현
