# 괴이기록국 · GPT Work Cold-Start Handoff

> Role: `GPT_WORK_COLD_START_HANDOFF`
> Created: `2026-08-26 KST`
> Project: `alsdmlals4-eng/urban-legend`
> Project main snapshot at handoff: `b2524dcd805f59f0332dd4f12dde451c5a4f5338`
> Base main snapshot at handoff: `06669fe9c6a3ccd6f3b0d19c5757540bfdcc0623`
> User execution surface after handoff: `ChatGPT Work`
> Current task family: `PLANNING / VISUAL-ASSET REQUIREMENT / IMAGE WORK`
> Product implementation: `PAUSED_UNTIL_EXPLICIT_SEPARATE_AUTHORIZATION`
> Human QA: `NOT_RUN`
> Product asset approval: `0 CURRENT ROOT-MANIFEST ENTRIES`

## 0. Why this handoff exists

The user is moving continuing project work from ordinary Chat into **ChatGPT Work**.

This file is a cold-start pointer, not a replacement for fresh-read authority. Work must reconstruct current truth from current GitHub/Notion/Base before acting.

Do not assume this file, prior chat, memory, uploaded files, or old screenshots are still current if repository or Notion has moved.

## 1. Work startup sequence — mandatory

At the beginning of a new Work thread:

1. Fresh-read Base repository `alsdmlals4-eng/Base` current `main` root structure and latest completed commit.
2. Fresh-read project repository `alsdmlals4-eng/urban-legend` default branch, latest completed `main`, current root, and open PRs.
3. Fresh-read Notion `괴이기록국 · Home` and the relevant current domain page, especially `04 · Visual · UX · Assets` for visual work.
4. Fresh-read the legacy Google Sheet only to detect migration/history conflicts. It is not current authority.
5. Read this handoff plus the current project owner documents listed below.
6. If GitHub and Notion disagree, stop mutation and report `CONTEXT_DRIFT_RECHECK_REQUIRED` before deciding.
7. Treat all pre-existing open/draft/ready PRs as read-only unless the user explicitly names one and authorizes mutation.

### Base / Work routing boundary

Base `main` already records the routing shape:

```text
Chat  → quick discussion / decision shaping
Work  → long multi-step research, review, Notion/Base/document/analysis execution
Codex → actual game product implementation
```

Work is an execution surface, not a new source of truth. Notion remains human-facing planning/visual authority and GitHub remains structured/runtime authority.

At this handoff, Base PR `#726 feat: add Work-native project execution instruction` is **open draft**, not current authority. If a future Work session finds it merged into Base `main`, use the merged current-main owner. If it is still open, read-only only.

## 2. Authority snapshot at handoff

### Project GitHub

- default branch: `main`
- current completed `main`: `b2524dcd805f59f0332dd4f12dde451c5a4f5338`
- latest completed change: `docs: connect M04 approved anchor to runtime consumer (#237)`
- current project open PR: `#231 docs: align front doors with merged runtime canon`
- PR `#231` is an old draft workstream and remains **read-only**.

### Base

- current completed Base `main`: `06669fe9c6a3ccd6f3b0d19c5757540bfdcc0623`
- relevant recent Base rules already merged:
  - generated game images require real consumers;
  - long noncoding work routes through ChatGPT Work;
  - default project memory may assist discovery but stays below project canon and fresh-read evidence.
- open Base PRs are not authority. In particular `#726` is the Work-native instruction implementation draft at this snapshot.

### Notion

Primary human entry:

- `괴이기록국 · Home`
- page id: `3c41b237-eb1c-81dc-9bda-d72bf2d5978d`

Visual domain:

- `04 · Visual · UX · Assets`
- page id: `3c51b237-eb1c-81ae-abf2-f94a5146aa0f`

### Legacy Google Sheet

The linked Sheet still reports the old `2026-08-11` state, including obsolete project/Base SHAs and obsolete PR sets.

State: `STALE / MIGRATION_ONLY / NON_AUTHORITY`.

Do not write current planning back into the Sheet by default.

## 3. Current product/planning canon

Current loop:

```text
주간 일정·육성
→ 월간 사건 징후·출동
→ 조사
→ 추리·괴이 매뉴얼
→ 피해자 구출
→ 전조 기반 회수
→ 복합 결과
→ 후일담·연구·관계
→ 다음 달 사건
```

Result authority: `COMPOSITE_RESULT`.

A single S/A/B grade must not overwrite incident outcome. Legacy grade/S-rank is compatibility/history/mastery only.

Current six independent result axes:

1. `victim_outcome`
2. `control_or_stabilization`
3. `evidence_integrity`
4. `protection_responsibility`
5. `residual_anomaly`
6. `unresolved_and_follow_up`

M01 `저승역` is onboarding/first-session/regression.
M04 `비 오는 골목의 빨간 우산` is the 30–45 minute release-near vertical slice.

## 4. Current visual language

Locked treatment:

- `SOFT_ANIME_NOIR_LOCKED`
- `DOSSIER_HYBRID_IS_PRESENTATION_LANGUAGE_NOT_MEDIUM`

Visual continuity:

- field/Recovery: black/charcoal, restrained distressed gold, restrained danger red and protection green;
- manual/rescue/records: aged ivory/parchment, ink/pencil, maps, stamps, dossier/archive feel;
- characters: one step more anime-styled than environment/document framing, but must not dominate general investigation hierarchy;
- pixel/dot language: secondary observation language for CCTV, sensor, log, map, interference.

## 5. Recovery successor — do not revive predecessor

Decision: `D-2026-08-25-RECOVERY-CONTEXT-ACTION-HIERARCHY`.

Current priority:

1. anomaly / manifestation
2. next telegraph
3. protected target
4. reference rule / expected impact
5. contextual telegraph response
6. persistent base categories `공격 / 보호 / 보조`
7. allies/resources

Persistent first-level action categories are only:

```text
공격 / 보호 / 보조
```

Each opens second-level actions.

Contextual world actions generated by the current telegraph are separate, for example movement, sign manipulation, broadcast-device operation, door operation.

**Never revive the old flat seven-command UI** (`보호/관찰/대응/공격/장비/봉쇄/후퇴`). If an older current-planning line still mentions that predecessor, the Recovery decision and `CURRENT_VISUAL_WORK_ORDER` are the successor authority.

Approved Recovery revised full mockup status:

`USER_APPROVED_VISUAL_CANDIDATE / NOT_PRODUCT_ASSET`.

The approved full mockup is visual/reference evidence. It is not automatically `afterlife_recovery.png`, anomaly cutout, product asset, runtime PASS, or Human PASS.

## 6. Composite Result visual state

- Stage 1 — six-axis information hierarchy: `USER_APPROVED_REFERENCE`.
- Stage 2 — causal/rationale trace: `USER_APPROVED_REFERENCE`.
- Stage 3 — record consequence / failure observation / follow-up: `GENERATED / IN_REVIEW`.

Do not infer Stage 3 approval from later conversation.

The current `result_scene.gd` is Control/Theme-built, so these mockups are UI/visual direction references, not product PNG backlog items.

## 7. Runtime-consumer-first image rule

Decision: `D-2026-08-26-RUNTIME-CONSUMER-FIRST-VISUAL-ASSET-GATE`.

The user explicitly rejected explanation sheets as the image-production unit.

Required pipeline:

```text
actual Scene / Texture consumer
→ current tracked file / reusable approved candidate
→ Delete Test
→ REUSE_REVIEW | REPLACE_REQUIRED | CREATE_REQUIRED
→ one consumer-specific text brief
→ explicit user approval to generate
→ exactly one image
→ STOP for result approval
→ product promotion/runtime validation as separate gates
```

Do not generate images for UI that is currently Godot `PanelContainer`, `Label`, `Button`, `GridContainer`, Theme/StyleBox unless a real texture consumer is separately proven.

Therefore the following are **not image backlog** by default:

- Recovery Telegraph Badge explanation sheet;
- Recovery Context Action List sheet;
- Attack/Protect/Support Category Bar sheet;
- Composite Result Axis Card / causal strip / record-consequence tag sheets;
- generic public UI component explanation sheets.

## 8. Current actual image-consumer queue

There is currently **no proven `CREATE_REQUIRED` item**.

Most consumers already have tracked files and must remain `REUSE_REVIEW` until the pixels are actually compared.

### M01

| ID | Consumer | Current file | State |
|---|---|---|---|
| `IMG-M01-03` | Recovery `ArtLayer/Background` | `assets/backgrounds/afterlife_recovery.png` | `REUSE_REVIEW / PIXEL_COMPARE_PENDING` |
| `IMG-M01-04` | Recovery `AnomalyVisual` B/C | `afterlife_b_cutout.png` + full fallback | `REUSE_REVIEW / PIXEL_COMPARE_PENDING` |
| `IMG-M01-05` | Recovery `AnomalyVisual` D | `afterlife_d_cutout.png` + full fallback | `REUSE_REVIEW / PIXEL_COMPARE_PENDING` |
| `IMG-M01-02` | Investigation `ArtLayer/Background` + `LocationPreview` | `assets/backgrounds/afterlife_platform.png` | `REUSE_REVIEW / PIXEL_COMPARE_PENDING` |
| `IMG-M01-01` | Dialogue/location background + secondary Main Menu reuse | `assets/backgrounds/afterlife_entrance.png` | `REUSE_REVIEW / SHARED_CONSUMER_ROLE_REVIEW / PIXEL_COMPARE_PENDING` |

Important requirements already written:

- `docs/visual/IMG_M01_03_AFTERLIFE_RECOVERY_BACKGROUND_REQUIREMENT.md`
- `docs/visual/IMG_M01_RECOVERY_ANOMALY_STATE_FAMILY_REQUIREMENT.md`
- `docs/visual/IMG_M01_02_AFTERLIFE_INVESTIGATION_BACKGROUND_REQUIREMENT.md`
- `docs/visual/IMG_M01_01_AFTERLIFE_ENTRANCE_BACKGROUND_REQUIREMENT.md`

Recovery anomaly B/C and D are the **same anomaly identity at escalating risk stages**. Current texture selection is risk-stage based, not Recovery-pattern based. Do not bake the correct response to `목적지 합창 / 회귀 승강장 / 무정차 환송` into these PNGs.

### M04

`IMG-M04-02` actual consumer:

- Investigation `ArtLayer/Background`
- same texture reused by `LocationPreview`
- current tracked file: `assets/backgrounds/red_crossroads.png`
- approved challenger: M04 Investigation Anchor 1
- state: `USER_APPROVED_VISUAL_CANDIDATE / RUNTIME_CONSUMER_COMPARE_PENDING / PRODUCT_REFERENCE_ASSET_PENDING`

Owner:

- `docs/visual/IMG_M04_02_RED_CROSSROADS_PROMOTION_REQUIREMENT.md`

Compare outcomes only:

```text
REUSE_CURRENT
PROMOTE_CANDIDATE
ADAPT_CANDIDATE
```

The approved five layer goals (`BG_ENVIRONMENT / ANOMALY_UMBRELLA / EVIDENCE_FOOTPRINTS / ATMOSPHERE_RAIN / INTERFERENCE_OPTIONAL`) are **not a five-image generation quota**. There is not yet evidence of five separate runtime Texture consumers.

## 9. Product asset / evidence ceiling

Root `ASSET_MANIFEST.yml` is the current tracked product-asset approval authority.

At handoff:

```text
assets: []
PROJECT_ASSET_APPROVED: 0
```

Tracked PNG presence, `.import` files, legacy `assets/ASSET_MANIFEST.json`, user-approved visual mockups, automated CI, and Notion attachments do not independently grant product asset approval.

Still pending:

- current binary pixel review for the M01 reuse queue;
- product-reference promotion;
- rights/provenance promotion review;
- 1280×720 / 1920×1080 runtime readability;
- M01 actual Human first-session QA;
- M04 release-near visual/audio/VFX Human QA;
- Android;
- `POC_PASSED` declaration;
- production expansion.

## 10. Immediate next safe action in Work

1. Fresh-read current authority again; do not trust the snapshot SHA blindly.
2. If the current `afterlife_recovery.png` can be visually inspected in Work, compare it against the approved Recovery successor reference and `IMG_M01_03` requirement.
3. If it cannot be visually inspected, keep `REUSE_REVIEW`; request a user upload only when that pixel comparison becomes the actual blocker.
4. Do **not** classify `REPLACE_REQUIRED` from inference.
5. If `REPLACE_REQUIRED` is proven, prepare one text brief for the actual Recovery background consumer and wait for explicit generation approval.
6. Generate exactly one image only after that approval, then stop for result approval.
7. Continue the same process for M01 anomaly B/C → D → Investigation → Entrance, then M04 `red_crossroads` compare/promotion gate.

## 11. Current user scope boundary

The user explicitly moved the project into **planning and image work first**.

Do not start Codex/Godot implementation automatically.

Actual game product implementation belongs to Codex only after a separate approved implementation handoff. Work may perform planning, audits, authority synchronization, image requirement work, Notion/GitHub documentation, and finished noncoding deliverables.

## 12. First message to use in a new Work thread

A minimal cold-start request is enough:

> 프로젝트 `괴이기록국(urban-legend)` 이어서 진행. 먼저 Base main, 프로젝트 GitHub main/open PR, Notion Home/Visual, legacy Sheet를 fresh-read하고 `docs/handoff/GPT_WORK_HANDOFF_2026-08-26.md`를 읽어 현재 상태를 재구성해. 과거 채팅/메모리는 discovery-only로 보고 current authority와 충돌하면 버려. 지금은 기획·이미지 작업만 진행하고 Codex/Godot 구현은 별도 승인 전 시작하지 마. 이미지 제작은 실제 runtime consumer 기준이며 한 단계당 텍스트 브리프 승인 → 이미지 정확히 1장 → 결과 승인 순서를 지켜.

## 13. Current owner documents

Read these after fresh-read bootstrap:

- `docs/CURRENT_PLANNING_CANON.md`
- `docs/CURRENT_VISUAL_WORK_ORDER.md`
- `docs/CURRENT_VISUAL_ASSET_CONSUMER_CHECKLIST.md`
- `docs/decisions/D-2026-08-25-RECOVERY-CONTEXT-ACTION-HIERARCHY.md`
- `docs/decisions/D-2026-08-26-RUNTIME-CONSUMER-FIRST-VISUAL-ASSET-GATE.md`
- `docs/visual/IMG_M01_03_AFTERLIFE_RECOVERY_BACKGROUND_REQUIREMENT.md`
- `docs/visual/IMG_M01_RECOVERY_ANOMALY_STATE_FAMILY_REQUIREMENT.md`
- `docs/visual/IMG_M01_02_AFTERLIFE_INVESTIGATION_BACKGROUND_REQUIREMENT.md`
- `docs/visual/IMG_M01_01_AFTERLIFE_ENTRANCE_BACKGROUND_REQUIREMENT.md`
- `docs/visual/IMG_M04_02_RED_CROSSROADS_PROMOTION_REQUIREMENT.md`
- `ASSET_MANIFEST.yml`

## 14. Known conflict at handoff

The legacy Google Sheet conflicts with current GitHub/Notion and must not drive the next Work task.

A separate stale wording also existed in `CURRENT_PLANNING_CANON.md` describing the predecessor flat seven-command Recovery set. The handoff workstream is expected to reconcile that wording to the already approved `D-2026-08-25-RECOVERY-CONTEXT-ACTION-HIERARCHY` before merge so a future Work cold-start does not revive it.
