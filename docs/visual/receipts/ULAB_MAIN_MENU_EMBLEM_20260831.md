# ULAB Main Menu Emblem — User-Locked Receipt

## Identity and state

- `asset_id`: `ULAB-MAIN-MENU-EMBLEM-001`
- `state`: `PROJECT_ASSET_APPROVED → IMPLEMENTED → RUNTIME_VERIFIED`
- `canonical_path`: `assets/ui/bureau_archive_emblem.png`
- `pixel_size`: `1224 × 1285`
- `bytes`: `1,710,284`
- `sha256`: `2c8722ca8c5e73140752b9fb4a540c672835315464b8b14cab5daa0448982d0c`
- `alpha_boundary_check`: each corner pixel is alpha `0`

## Approval and intended consumer

On 2026-08-31, the user selected the first previously generated emblem candidate with the instruction, “로고는 1번째 걸로쓰자.” This locks only the compact emblem for the main-menu identity lockup; it does not lock a replacement title treatment, a scenario-specific emblem, or a separate word-typography candidate.

The actual consumer is `scripts/ui/main_menu.gd`, which adds `WorldTitleEmblem` to the existing `WorldTitleLockup`. The icon is a non-interactive `TextureRect` with a 72 × 72 layout box, `KEEP_ASPECT_CENTERED`, and ignored pointer input. Native Godot labels continue to own the Korean title, subtitle, focus order, and accessible text.

## Provenance and rights boundary

The selected source was generated by the OpenAI built-in image generation surface and was delivered as `exec-e0f90e1d-4e28-4c12-b310-f759a0034360.png`. The original prompt and model identifier were not retained in the repository, so they are not reconstructed here. The repository canonical file is an exact SHA-256-matched copy of that selected candidate.

This receipt records the user selection, current consumer, hash, and an obvious-conflict boundary. It is not a platform-distribution, copyright, trademark, or legal clearance. No named third-party logo, character, or source image is asserted as a production input.

## Verification boundary

Machine/runtime verification is limited to the main-menu contract at `1280 × 720` and `1920 × 1080`, including node presence, non-null texture, and viewport containment. The actual `1280 × 720` runtime capture is stored at `docs/qa/captures/main-menu/bureau-emblem-20260831/main-menu-emblem-1280x720.png`; `1920 × 1080` is covered by the live-scene contract rather than a stored image capture. Human visual QA, accessibility QA, and release-rights review remain `NOT_RUN`.
