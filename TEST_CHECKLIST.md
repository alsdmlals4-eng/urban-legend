# TEST_CHECKLIST

> 문서 위치: `TEST_CHECKLIST.md` | 완료 상세: `docs/qa/` | 상태 원본: `docs/CURRENT_STATUS.md`

## 목적

현재 구현 기준인 **MVP-043 + CORE-VALIDATION-001 + UX-PD-001 2A / Ver 4.2 / 저장 스키마 `mvp-039`**를 보호하면서 선택한 다음 변경만 검증한다. 실행하지 못한 항목은 PASS로 보고하지 않는다.

## 공통 자동 검증

```powershell
& "C:\Users\user\Downloads\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe" --headless --path . --quit
git diff --check
& "C:\Users\user\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe" tools/docs/build_game_design_doc.py --check
```

- [ ] 프로젝트 import와 변경 씬 로드가 통과한다.
- [ ] 변경한 JSON·씬·스크립트의 전용 테스트가 통과한다.
- [ ] `tests/run_godot_regression.sh` 전체 진입점이 통과한다.
- [ ] 테스트 저장은 실제 사용자 저장을 덮어쓰지 않는다.
- [ ] GDD를 바꿨다면 Markdown과 DOCX를 재생성·검증한다.
- [ ] `git diff --check`와 활성 문서 참조 감사가 통과한다.

## 핵심 회귀 계약

### 저장·캠페인

- [ ] `mvp-039` 저장과 `mvp-038` 이관이 유지된다.
- [ ] 권나래가 주인공이며 서포트는 최대 2명이다.
- [ ] 오전 결과 확인 전 오후 계획으로 넘어가지 않는다.
- [ ] HQ 중단·저장·불러오기·재개로 반일이 중복 완료되지 않는다.
- [ ] 난수·지원 시도·게시판 상태가 불러오기로 재추첨되지 않는다.

### 사건·추리

- [ ] 세 사건의 기존 장면·선택·회수 ID가 유지된다.
- [ ] 실패가 위험 사례와 다음 판단 근거를 남긴다.
- [ ] 현장 대응 성공과 공식 매뉴얼 승격이 분리된다.
- [ ] 요원·아카·장비·UI가 미확보 단서와 정답을 대신 제공하지 않는다.
- [ ] 괴이는 처치하지 않고 안정화 상태와 잔향 회수로 종료된다.

### UI·접근성

- [ ] 1280×720과 1920×1080에서 핵심 선택과 상태가 화면 안에 있다.
- [ ] 긴 한국어 문장이 잘리지 않는다.
- [ ] 키보드 포커스, Esc/닫기, 마우스 입력이 충돌하지 않는다.
- [ ] 음향·색상만으로 중요한 상태를 전달하지 않는다.
- [ ] UI가 저장·진행 결과를 중복 소유하지 않는다.

## UX-PD-001 2A 조건부 검증

- [ ] 신규 상태에서 사건·편성 탭이 항상 보인다.
- [ ] 장비·외부 접점·기록 탭이 최초에는 접힌다.
- [ ] `보조 준비 도구` 한 번으로 기존 기능 전체가 즉시 복원된다.
- [ ] 공개 선택이 기존 `seen_hint_ids`에 저장된다.
- [ ] 완료 사건·해금 장비·기록·장착 장비·소모품·수락 의뢰가 있는 진행 저장은 자동 공개된다.
- [ ] 시장·장비·세력 의뢰·소모품·기록의 데이터·가격·효과가 바뀌지 않는다.
- [ ] 기존 `preparation_scene.gd`가 일정·편성·경제 상태를 계속 소유한다.

## UX-PD-001 2B 진입 전 확인

- [ ] `investigation_scene.gd`의 상태 소유자와 화면 파생 정보를 구분했다.
- [ ] 현재 관측·선택과 과거 로그·위험 사례·상세 상태의 우선순위를 정의했다.
- [ ] 미확보 정보와 정답 추론을 새 UI가 노출하지 않는다.
- [ ] 한 사건의 첫 장면부터 선택까지 작은 수직 단위로 범위를 제한했다.

## 문서 감사

- [ ] README·CURRENT_STATUS·MVP_ROADMAP·책임 기획서의 기준선이 일치한다.
- [ ] 구현되지 않은 계획을 완료 기능으로 쓰지 않았다.
- [ ] 링크와 파일 경로가 유효하다.
- [ ] 완료된 Goal·QA·백업·구형 브랜치·임시 워크플로를 현행 책임 원본으로 참조하지 않는다.

## 완료 보고

- 변경 파일과 이유
- 플레이어가 확인할 변화
- 자동·수동 검증 결과
- 미검증 항목과 위험
- 저장·UI·데이터 호환 결과
- 갱신한 현행 문서
- 다음 구현 진입점
